#include "main.hpp"
#include <iostream>

int main(int argc, char const *argv[]) {
    (void)argv;
    (void)argc;

    // Setup empty data structures
    std::vector<Server> authServers;
    std::vector<GameServer> gameServers;
    std::vector<UserSession> connectedUsers;

    // Setup listeners
    std::cout << "Hello there!";
}